package com.codesync.project.model;

/**
 * Project visibility setting.
 * PUBLIC  – anyone authenticated can view the project.
 * PRIVATE – only the owner and explicit members can access it.
 */
public enum Visibility {
    PUBLIC,
    PRIVATE
}
